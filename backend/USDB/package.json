{
  "name": "usdb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node builds/index.js",
    "build": "npm run cleanup && tsc index.ts --outDir builds",
    "prod": "npm run cleanup && tsc index.ts --outDir builds && node builds/index.js",
    "cleanup": "rm -rf builds",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.17.0"
  },
  "dependencies": {
    "@types/express": "^4.17.0",
    "express": "^4.17.1",
    "typescript": "^3.5.1"
  }
}
